"""benchmark — Role-specific LLM benchmark tool for the agent.

Usage:
    python -m benchmark --role router --depth quick
    python -m benchmark --group executor --depth standard --runs 3
    python -m benchmark --compare granite-350m,qwen-3b --role code
"""
__version__ = "1.1.0"

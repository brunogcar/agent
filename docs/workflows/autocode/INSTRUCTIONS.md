<- Back to [Autocode Overview](../AUTOCODE.md)

# 🛡️ AI Instructions

## ❌ NEVER DO

1. **Never mutate state in-place** — LangGraph does not deep-copy. Always return partial update `dict`s.
2. **Never spread `**state`** — Never return `{**state, "key": "value"}`. Return only the changed keys.
3. **Never remove TDD-first** — Test generation ensures code quality.
4. **Never remove debug loop** — Iteration catches edge cases.
5. **Never remove impact analysis** — Blast radius analysis prevents unintended side effects.
6. **Never use `print()` to stdout** — MCP stdio corruption. Use `tracer.step()` for logging.
7. **Never create `.bak` files** — forbidden by project rules.
8. **Never rewrite the entire file** — surgical edits only. Preserve existing code exactly.
9. **Never skip `compileall` before `pytest`** — catches syntax errors early.
10. **Never call `agent()` without `action="dispatch"`** — The `agent()` facade requires `action`. Always pass `action="dispatch"` for LLM calls.
11. **Never return `None` from LangGraph nodes** — Always return a `dict` (even empty `{}`).
12. **Never use `files_context` field** — It doesn't exist in `AutocodeState`. Use `_files_context()` helper.
13. **Never store `files_map` as `dict[str, FileSnapshot]`** — No node populates it. Use `modified_files` instead.
14. **Never import removed symbols in the facade** — [v1.1] The facade was broken for 2 versions because it imported `AGENT_ROOT`, `route_after_brainstorm`, `route_after_debug`, `_git_snapshot` after they were removed from their source modules. When removing a symbol from `state.py`/`routes.py`/`git_ops.py`, grep the facade and remove the import + `__all__` entry too.
15. **Never call `.compile()` on an already-compiled graph** — [v1.1] `get_graph()` returns a `CompiledStateGraph` which has no `.compile()` method. Calling it crashes with `AttributeError`. Use `get_graph()` directly, or `build_graph().compile()` for a fresh compile.
16. **Never let `distill_memory` fail the workflow** — [v1.1] The code is already committed by the time distill_memory runs. A ChromaDB failure there must not flip a successful workflow to failed. Use `tracer.warning`, not `tracer.error`, and return `{}`.
17. **Never bypass `base.py`'s `run_workflow()` from the facade** — [v1.1] The facade delegates to `run_workflow("autocode")` for tracing, checkpointing, and timeout. Bypassing it (the old design) caused double trace creation and missing checkpoint support.
18. **Never put push / PR / merge logic in `node_commit` — use `node_publish`** — [v1.3] `node_commit` is local-only (calls `git(action="commit")` via `git_ops.py`). ALL remote operations (push, pr_create, pr_comment, pr_merge) live in `node_publish` via `github_ops.py`. Folding them would couple commit failure semantics with publish failure semantics and would force every autocode run to import the github tool even when only local git is needed. See ARCHITECTURE.md § "[v1.3] Design Decision Notes" #1.
19. **Never call GitHub API actions without `is_configured()`** — [v1.3] Every helper in `workflows/autocode_impl/github_ops.py` MUST call `_github_is_configured()` (wraps `tools.github_ops.client.is_configured()` in try/except) before any `github(action=...)` call. If GitHub is not configured (`GITHUB_TOKEN` / `GITHUB_OWNER` / `GITHUB_REPO` missing), log a `tracer.step` and return `False`/`None` — never raise. This is what makes the v1.3 integration safe to deploy with no `.env` changes.
20. **Never add a v1.3 integration flag that defaults ON** — [v1.3] All 6 new flags (`AUTOCODE_PULL_BEFORE_BRANCH`, `AUTOCODE_PUSH_ON_COMMIT`, `AUTOCODE_OPEN_PR`, `AUTOCODE_AUTO_MERGE`, `AUTOCODE_DEBUG_COMMENT_PR`, `AUTOCODE_SWARM_DEBUG`) default OFF. With all flags OFF, autocode v1.3 is byte-for-byte behaviorally identical to v1.2. New integration flags MUST default OFF so existing installations are not surprised by remote API calls or auto-merges after upgrade. `# TODO(2.0):` Per-task overrides may relax this.
21. **Never block the debug loop on a swarm verdict** — [v1.3] Swarm debug (`AUTOCODE_SWARM_DEBUG=1`) is non-blocking by design. The fix is ALWAYS applied, regardless of confidence (HIGH/MEDIUM/LOW). LOW confidence surfaces as an optional PR comment (gated on `AUTOCODE_DEBUG_COMMENT_PR=1` + `state["pr_number"]`), NOT as a workflow block. Rationale: the debug loop already has `MAX_RETRIES`, stuck-detection routing, the `node_verify` gate, and the git branch as safety nets; blocking on a multi-LLM vote would add latency and a new failure mode without improving correctness. See ARCHITECTURE.md § "[v1.3] Design Decision Notes" #3. `# TODO(2.0):` `AUTOCODE_SWARM_BLOCK_ON_LOW_CONFIDENCE` may relax this.
22. **Never call `github_ops.py` helpers from outside `nodes/`** — [v1.3] The helpers (`_github_pull`, `_github_push`, `_github_pr_create`, `_github_pr_comment`, `_github_pr_merge`, `_swarm_debug_consensus`) are private (underscore-prefixed) and intended to be called only from `nodes/branch.py`, `nodes/publish.py`, `nodes/debug.py`, `nodes/push.py`, `nodes/create_pr.py`, and `nodes/merge_pr` (**[v2.0-beta]** Phase 3.3 split — `publish.py` is now a wrapper, the 3 split nodes call the helpers directly). Other modules should call the `github()` / `swarm()` facades directly if they need remote operations. This keeps the lazy-import + `is_configured()` + `tracer.step` pattern in one place.
23. **Never mix `git_ops.py` and `github_ops.py` helpers in one node** — [v1.3] `git_ops.py` = local git (no network, no auth). `github_ops.py` = remote GitHub (requires `is_configured()`). Exception: `node_git_branch` calls BOTH (local `_git_create_branch` + optional remote `_github_pull`) — this is the ONLY sanctioned mixing point because pull happens immediately before branch creation. `node_publish` is remote-only. `node_commit` is local-only. If you find another node needing both, split it.
24. **Never use raw `json.loads()` on LLM-generated output** — **[Pre-2.0 Fix]** LLMs frequently wrap JSON in markdown fences (```` ```json\n{...}\n``` ````). Raw `json.loads()` raises `JSONDecodeError` and the workflow silently drops the parsed data (e.g., `modified_files` was always `[]` in `node_execute_step`). ALWAYS use `_parse_json(text)` from `helpers.py` which strips fences before parsing. Found by: MiMo (P1) — see CHANGELOG.md § "Cross-LLM Review Findings (Pre-2.0)".
25. **Never write LLM-generated paths to disk without `_is_path_safe()`** — **[Pre-2.0 Fix]** The `node_validate_input` path traversal check only covers USER-supplied paths. LLM-generated paths (`patches[].path`, `new_files{}` keys) are untrusted — an LLM hallucination like `"../../etc/passwd"` would escape `base_path`. ALWAYS call `_is_path_safe(base_path, rel_path)` (now in `nodes/apply_patches.py` — **[v2.0-beta]** Phase 3.1 moved it from `nodes/write_files.py`; imported by `write_new_files.py` via `from workflows.autocode_impl.nodes.apply_patches import _is_path_safe`) before constructing `base_path / rel_path` from LLM output. Found by: Qwen (P0.1). `# TODO(2.0):` Consider centralizing all path-safety checks in `core/path_guard.py`.
26. **Never re-add `node_write_files_with_flag_reset`** — **[Pre-2.0 Fix]** It was dead code: registered in the graph but never wired (the debug loop edges to `node_write_files` directly), and it reset `step_attempt` — a field that does NOT exist in `AutocodeState` or `_default_state()`. The real stuck-detection field (`last_test_error`) is correctly handled by `run_tests.py`. If you need a "retry with reset" node, use a real field and wire it properly. Found by: MiMo + Kimi (P0). See CHANGELOG.md.
27. **Never re-add `route_after_analyze_impact`** — **[Pre-2.0 Fix]** It was a conditional router that ALWAYS returned `"node_run_tests"`. Use a direct edge (`workflow.add_edge("node_analyze_impact", "node_run_tests")`) instead. Conditional edges are for branches with ≥2 possible targets. Found by: MiMo (P3). See CHANGELOG.md.
28. **Never re-add `mermaid.py`, `test_mapper.py`, or `test_runner.py` to `workflows/autocode_impl/`** — **[Pre-2.0 Fix]** All three were unused. `mermaid.py` was never called (`WORKFLOW_METADATA` serves the same purpose). `test_mapper.py` was shadowed by `core.kgraph.test_mapper` (the one `analyze_impact.py` actually imports). `test_runner.py` was shadowed by `node_run_tests`'s own logic. If you need a mermaid export, use `WORKFLOW_METADATA` to generate it on demand. Found by: Kimi. See CHANGELOG.md.
29. **Never call `tracer.error()` with 2 args** — **[Pre-2.0 Fix]** The signature is `tracer.error(trace_id, category, message)` (3 args). Passing only 2 args misattributed trace data — the message was being recorded as the category. ALWAYS pass all three: tid, category, msg. Found by: Qwen (P1). See CHANGELOG.md.
30. **Never assume `state["branch_name"]` is populated** — **[Pre-2.0 Fix]** `node_write_plan` writes to `state["branch"]` (not `branch_name`). The facade's `_shape_artifacts()` was reading `branch_name` → always `""`. ALWAYS use `final_state.get("branch_name") or final_state.get("branch", "")` as a fallback. Found by: MiMo + Kimi (P1). See CHANGELOG.md.
31. **Never use `s["label"]` on plan steps** — **[Pre-2.0 Fix]** LLM-returned plans are not guaranteed to label every step. ALWAYS use `s.get("label", "step")` (or another sensible default) to avoid `KeyError`. Found by: DeepSeek (P1) in `node_commit`. See CHANGELOG.md.
32. **Never remove legacy flat fields from `AutocodeState` yet** — **[v2.0]** Phase 2 adds 8 sub-state TypedDicts + accessor functions on TOP of the existing flat fields, but the flat fields are KEPT as the backward-compat fallback path. They are scheduled for removal in **Phase 6** — AFTER all 17 nodes have been migrated to use accessors (`commit.py` is the only migrated node so far). Removing a flat field now breaks every unmigrated node that reads it via `state.get(...)`. `# TODO(2.0):` Phase 6 removes the flat fields + the accessor fallback branches in one mechanical diff.
33. **Never bypass the accessor layer when writing NEW code that reads state** — **[v2.0]** New nodes (and edits to existing nodes) MUST read state via the 8 accessor functions (`_get_plan`, `_get_tdd`, `_get_files`, `_get_impact`, `_get_debug`, `_get_verify`, `_get_vcs`, `_get_memory`) instead of `state.get("flat_field", ...)`. The accessors transparently handle both the new sub-state dict shape (`state["vcs"]["branch"]`) and the legacy flat-field shape (`state["branch"]`), so callers don't need to know which shape the upstream writer used. See API.md § "[v2.0] State Accessors". Legacy reads in unmigrated nodes are tolerated until Phase 3 — do NOT bulk-rewrite them.
34. **Never wire the backward-compat wrappers (`node_write_files`, `node_verify`, `node_publish`) into the graph** — **[v2.0-beta]** Phase 3 split these 3 "god nodes" into 10 focused nodes (`apply_patches` / `write_new_files` / `persist_artifacts` for Phase 3.1; `run_pytest` / `run_lint` / `llm_review` / `verify_decision` for Phase 3.2; `push` / `create_pr` / `merge_pr` for Phase 3.3). The 3 originals are kept as backward-compat wrappers — registered via `add_node(...)` so external callers + tests that `import` them directly still work, but they have NO edges in or out (no `workflow.add_edge(...)` or `workflow.add_conditional_edges(...)` references them). The graph flow uses the split nodes directly: `execute → apply_patches → write_new_files → persist_artifacts → [route]`, `[route] → run_pytest → run_lint → llm_review → verify_decision → [route_after_verify]`, `commit → push → create_pr → merge_pr → distill_memory`. Wrappers are also EXCLUDED from `WORKFLOW_METADATA["nodes"]` (27-entry list — wrappers excluded) so MCP clients render only the 27 active nodes. NEVER add an edge that targets a wrapper as the destination, and NEVER add an edge from a wrapper as the source. `# TODO(2.0):` Phase 6 removes the 3 wrappers once all external callers have migrated to the split nodes.
35. **Never call `_call()` on the backward-compat wrappers in tests** — **[v2.0-beta]** Phase 3.2 split `node_verify` into 4 nodes; the LLM `_call(...)` invocation moved from `verify.py` to `llm_review.py` (the wrapper re-exports nothing — it just runs the 4 split nodes). Tests that previously did `patch("workflows.autocode_impl.nodes.verify._call", ...)` MUST now patch `workflows.autocode_impl.nodes.llm_review._call` instead — the wrapper doesn't re-export `_call`, so patching the old path silently no-ops. Same pattern will apply if any future split moves a callable into a different module: patch the module that ACTUALLY imports the callable, not the wrapper module.
36. **Never repeat a failed debug approach — check `debug_history` first** — **[v2.0-rc1]** Phase 4.2 made `node_systematic_debug` stateful across iterations: it reads `debug_history` from the `TDDState` sub-state via `_get_tdd(state, "debug_history", [])` and injects the last 5 entries into the LLM user prompt under a `--- PRIOR DEBUG ATTEMPTS (do NOT repeat these) ---` block. If you edit the debug prompt or the `_DEBUG_JSON_SCHEMA`, you MUST preserve this injection — without it, the LLM will re-propose the same failed hypothesis on every iteration and the architecture-question exit will fire prematurely. The entry shape is `{iteration, phase, root_cause, fix[:200], tests_passed}` (swarm path adds `confidence`). Never read `debug_history` via `state.get("debug_history", [])` directly — use the `_get_tdd` accessor so the legacy-flat-field fallback path stays intact until Phase 6.
37. **Never wire the new `node_summarize_context` as a wrapper or skip it in the debug loop** — **[v2.0-rc1]** Phase 4.3 added `node_summarize_context` as a FULLY-WIRED active node (NOT a backward-compat wrapper) between `node_systematic_debug` and `node_apply_patches`. The debug loop edges are now `debug → summarize_context → apply_patches` (was: `debug → apply_patches`). NEVER remove either edge — `node_summarize_context` is what compresses `debug_history` into `debug_summary` so the LLM context budget stays bounded (#37). It is included in `WORKFLOW_METADATA["nodes"]` (27-entry list — wrappers excluded but summarize_context IS included). If you split it or rename it, update both graph edges AND the `WORKFLOW_METADATA["loops"][0]["nodes"]` list (the `debug_loop` node list).
38. **Never call `helpers._write_files()` — it is DEPRECATED** — **[v2.0-rc2]** Phase 5.3 marked `helpers._write_files()` as DEPRECATED after a dead-code audit found it was never called by any node (`execute.py` imported it but never used the import — the dead import was removed: `from ...helpers import _call, _write_files, ...` → `from ...helpers import _call, _files_context, _parse_json`). The function is KEPT in `helpers.py` for backward compat with external importers (with a docstring pointer to the replacement), but new code MUST NOT call it. New nodes that need to write files MUST use the Phase 3.1 split nodes: `node_apply_patches` (for `str_replace` patches to existing files) + `node_write_new_files` (for new-file writes / overwrites) + `node_persist_artifacts` (for run-dir artifact persistence). Calling `_write_files()` from new code would silently reintroduce the dead-code path that Phase 5.3 just cleaned up. See API.md § "`helpers._write_files()` — [v2.0-rc2] DEPRECATED".

## ✅ ALWAYS DO

14. **Always return `dict` from nodes** — Not `AutocodeState`. Partial updates only.
15. **Always pass `trace_id` to tracer calls** — Observability requires trace correlation.
16. **Always handle validation failure gracefully** — Invalid input should return error state, not crash.
17. **Always test `node_classify_task` with mode override** — Assert correct task_type regardless of LLM output.
18. **Always test `node_brainstorm` with KG files** — Assert merged files (currently broken).
19. **Always test `node_analyze_impact` with empty files_map** — Assert early return (currently broken).
20. **Always test `node_write_files` with patch** — Assert atomic write and no `.bak` files.
21. **Always test `node_run_tests` with missing test files** — Assert error state.
22. **Always test `node_verify` with missing ruff** — Assert `lint_passed=False` (currently `True`).
23. **Always test `node_git_commit` with no changes** — Assert skipped state.
24. **Always test `node_distill_memory` with missing hypothesis** — Assert graceful handling.
25. **Always test `node_create_skill` with invalid name** — Assert error state.
26. **Always update this doc** when adding nodes, changing routing logic, or modifying error handling.
27. **Always use `defense_notes` (plural)** — The state field is `defense_notes`, not `defense_note`.
28. **Always use `asyncio.to_thread()` for CPU-bound work** — AST parsing and file I/O blocks the event loop.
29. **[v1.3] Always wrap `github_ops.py` helper calls in the node's natural skip-conditions** — `node_publish` checks `status in {needs_clarification, failed, skipped}`, `verification_passed`, `dry_run`, AND `branch` is non-empty BEFORE calling any `_github_*` helper. Mirror this guard pattern in any future node that touches remote APIs — never call a remote helper unconditionally.
30. **[v1.3] Always use `tracer.step()` (not `tracer.error()`) for graceful-skip events in `github_ops.py`** — `is_configured()` returning `False` is NOT an error — it's the documented opt-out path. Same for `_github_push` returning `False` when push fails (the workflow continues). Use `tracer.error()` only for genuine unexpected exceptions (e.g., `ImportError` on `from tools.github import github`).
31. **[v1.3] Always lazy-import `tools.github` and `tools.swarm` inside the helper function** — `github_ops.py` mirrors the `git_ops.py` pattern: `from tools.github import github` is INSIDE each `_github_*` function, not at module top. This keeps `workflows.autocode_impl` importable even if `tools.github` is uninstalled or its deps are missing. Top-level imports of optional tools in workflow modules are forbidden (see deep_research's `search.py` for the rare sanctioned exception).
32. **[v1.3] Always log the swarm verdict in `state.swarm_verdict`** — When `_swarm_debug_consensus()` returns a non-`None` result, `node_systematic_debug` MUST set `swarm_verdict` in its return dict (not just `root_cause`/`fix`). This is what `node_publish._build_pr_body()` reads to surface confidence/agreement in the PR body, and what `node_publish` uses to decide whether to add the LOW-confidence warning. Skipping this field silently breaks the PR-comment-on-LOW flow.
33. **[v1.3] Always gate `AUTOCODE_DEBUG_COMMENT_PR` on `state["pr_number"]`** — The PR-comment-on-LOW-confidence flow in `node_systematic_debug` checks THREE conditions: `confidence == "LOW"` AND `cfg.autocode_debug_comment_pr` AND `state.get("pr_number")`. All three are required. Posting a PR comment when `pr_number` is 0 would call `github(action="pr_comment", number=0, ...)` which fails uselessly. The `pr_number` field is only set by `node_publish`, which runs AFTER the debug loop in the normal flow — so the LOW-confidence comment fires only on subsequent debug iterations AFTER the first PR has been opened.
34. **[v1.3] Always use `# TODO(2.0):` markers for v1.3 tech debt** — The 2.0 Review Notes section in CHANGELOG.md lists the known tech debt items from v1.3 (git_ops/github_ops split, node_publish single-node, debug statelessness, non-blocking swarm verdict, global config flags, hardcoded squash merge, non-blocking pull failure, swarm confidence thresholds, minimal PR body, no retry on push/PR failure). Any future edit that touches these areas MUST reference the corresponding `# TODO(2.0):` line in the source file.
35. **[Pre-2.0 Fix] Always mark v1.4 fixes with `[Pre-2.0 Fix]` in source + docs** — The v1.4 release is a hardening pass based on a cross-LLM review (DeepSeek, Qwen, MiMo, Kimi). Each fix is marked `[Pre-2.0 Fix]` in the source code AND in the API.md / ARCHITECTURE.md / INSTRUCTIONS.md docs so future editors can trace the rationale. The CHANGELOG.md § "Cross-LLM Review Findings (Pre-2.0)" section documents which LLMs found which bugs. Any future edit that touches a `[Pre-2.0 Fix]` area MUST preserve the marker and reference the CHANGELOG.md entry.
36. **[Pre-2.0 Fix] Always use `_parse_json()` for LLM JSON output** — The helper in `helpers.py` strips markdown fences (```` ```json ... ``` ````) before calling `json.loads()`. Raw `json.loads()` on LLM output raises `JSONDecodeError` on fenced JSON and silently drops the parsed data. This applies to ALL nodes that parse LLM JSON: `node_execute_step`, `node_systematic_debug`, `node_brainstorm`, `node_verify`, `node_create_skill`. Found by: MiMo (P1).
37. **[Pre-2.0 Fix] Always validate LLM-generated paths with `_is_path_safe()`** — The helper in `nodes/apply_patches.py` (**[v2.0-beta]** Phase 3.1 moved it from `nodes/write_files.py`; imported by `write_new_files.py`) uses `Path.resolve().is_relative_to()` to verify the resolved target stays inside `base_path`. Call it BEFORE constructing `base_path / rel_path` from any LLM-generated path (`patches[].path`, `new_files{}` keys). The user-input check in `node_validate_input` is NOT sufficient — LLM output is untrusted. Found by: Qwen (P0).
38. **[Pre-2.0 Fix] Always include `trace_id` suffix in branch names** — `node_write_plan` MUST format branches as `autocode/{slug}-{tid_suffix}` where `tid_suffix = tid.replace("-", "")[:8]`. Without the suffix, two runs with the same task produce the same branch and the second run checks out the first run's branch (cross-contamination). Found by: MiMo + Kimi (P2). `# TODO(2.0):` Consider making this configurable.
39. **[Pre-2.0 Fix] Always use atomic writes (`tempfile` + `os.replace`) for skill files** — `node_create_skill` MUST write via `tempfile.NamedTemporaryFile(delete=False, suffix='.tmp')` followed by `os.replace(tmp_path, skill_path)`. Direct `write_text()` can leave a partially-written file if the process crashes mid-write. The same pattern is already used in `node_write_new_files` (**[v2.0-beta]** Phase 3.1 split — was `node_write_files` before Phase 3.1; since v1.0). Found by: MiMo (P2).
40. **[Pre-2.0 Fix] Always handle `tdd_status="stuck"` in the verify chain** — `route_after_run_tests` routes `"stuck"` (same error signature on consecutive iterations) to the verify chain, skipping the doomed debug loop. The verify chain MUST check `tdd_status in ("max_retries_exceeded", "stuck")` and return early with `verification_passed: False` — otherwise it proceeds as if tests had passed. Found by: DeepSeek (P1). **[v2.0-beta]** Phase 3.2 moved this check from `node_verify` into `node_verify_decision` (the last of the 4 split verify nodes) — when editing, patch the right node.
41. **[Pre-2.0 Fix] Always skip `pytest` when no test files exist** — The verify chain MUST check `tests_dir.exists() or test_file.exists()` BEFORE invoking pytest. Running `pytest` with no args executes the entire project test suite (thousands of tests, false failures, minutes of waste). Found by: DeepSeek (P0). **[v2.0-beta]** Phase 3.2 moved this check from `node_verify` into `node_run_pytest` (the first of the 4 split verify nodes).
42. **[Pre-2.0 Fix] Always scope `ruff check` to `modified_files` only** — The verify chain MUST pass the modified file paths as explicit args to `ruff check` (e.g., `ruff check path1 path2 ...`). Running `ruff check workspace_root` lints the entire workspace — slow and surfaces pre-existing lint errors elsewhere as failures. Found by: DeepSeek, Qwen, Kimi (P2 — 3 LLMs independently flagged this). **[v2.0-beta]** Phase 3.2 moved this check from `node_verify` into `node_run_lint` (the second of the 4 split verify nodes).
43. **[Pre-2.0 Fix] Always align prompt field names with state field names** — When editing a SYSTEM prompt in `constants.py` that asks the LLM to return JSON keys, the keys MUST match the `AutocodeState` TypedDict AND the `_DEBUG_JSON_SCHEMA` (or equivalent) exactly. The v1.4 `DEBUG_SYSTEM` prompt was using `hypothesis` / `defense_note` while the code read `root_cause` / `defense_notes` — swarm debug's `root_cause` was always `"Unknown"`. Found by: MiMo (P0).
44. **[v2.0] Always use accessor functions (`_get_tdd`, `_get_vcs`, etc.) for state reads in new code** — The 8 accessor functions in `state.py` (`_get_plan`, `_get_tdd`, `_get_files`, `_get_impact`, `_get_debug`, `_get_verify`, `_get_vcs`, `_get_memory`) handle the sub-state / legacy flat-field fallback transparently. New nodes and edits to existing nodes MUST call `_get_vcs(state, "branch", "main")` rather than `state.get("branch", "main")`. This is the foundation of the 7-phase refactor — `node_git_commit` is the proof-of-concept migration. See API.md § "[v2.0] State Accessors" and ARCHITECTURE.md § "[v2.0] Sub-state Architecture". `# TODO(2.0):` Migrate remaining 16 nodes during Phase 3 / Phase 4.
45. **[v2.0] Always mark v2.0 changes with `[v2.0]` in source comments and docs** — Every v2.0-phase edit (Phase 1 through Phase 7) MUST be tagged `[v2.0]` in the source comment AND in the corresponding API.md / ARCHITECTURE.md / INSTRUCTIONS.md / CHANGELOG.md entry. This mirrors the `[Pre-2.0 Fix]` marker convention from v1.4 and lets future editors trace which phase a change belongs to. Use `# TODO(2.0):` for forward-looking items that a later phase will address (e.g., `# TODO(2.0):` Phase 6 removes the legacy flat fields).
46. **[v2.0] Always delegate LLM JSON parsing to `core/json_extract.py`** — The new `core/json_extract.py` module is the single source of truth for all LLM JSON parsing. It exposes 3 functions: `extract_json(text)`, `extract_json_array(text)`, `extract_first_json(text)`. The legacy `helpers._parse_json` and `router._extract_first_json` now delegate to this module. New code that parses LLM JSON MUST import from `core/json_extract.py` directly (not from `helpers` or `router`) — the legacy wrappers are kept only for backward compat with existing callers. Do NOT roll a new JSON extraction helper in any other module.
47. **[v2.0-beta] New code MUST import the split nodes directly, not the wrappers** — Phase 3 split `node_write_files` / `node_verify` / `node_publish` into 10 focused nodes (see NEVER DO #34 for the full mapping). New code (new nodes, new tests, new graph edges, new doc references) MUST import + reference the split nodes by their actual module paths:
    - `from workflows.autocode_impl.nodes.apply_patches import node_apply_patches, _is_path_safe` (Phase 3.1 — `_is_path_safe` lives here)
    - `from workflows.autocode_impl.nodes.write_new_files import node_write_new_files` (Phase 3.1)
    - `from workflows.autocode_impl.nodes.persist_artifacts import node_persist_artifacts` (Phase 3.1)
    - `from workflows.autocode_impl.nodes.run_pytest import node_run_pytest` (Phase 3.2)
    - `from workflows.autocode_impl.nodes.run_lint import node_run_lint` (Phase 3.2)
    - `from workflows.autocode_impl.nodes.llm_review import node_llm_review` (Phase 3.2 — `_call` is imported here for the LLM spec check; tests that need to mock `_call` MUST patch `workflows.autocode_impl.nodes.llm_review._call`)
    - `from workflows.autocode_impl.nodes.verify_decision import node_verify_decision` (Phase 3.2)
    - `from workflows.autocode_impl.nodes.push import node_push` (Phase 3.3)
    - `from workflows.autocode_impl.nodes.create_pr import node_create_pr, _build_pr_body` (Phase 3.3 — `_build_pr_body` lives here now; was in `publish.py` before Phase 3.3)
    - `from workflows.autocode_impl.nodes.merge_pr import node_merge_pr` (Phase 3.3)

    The wrappers (`node_write_files` / `node_verify` / `node_publish`) are kept ONLY for backward compat with existing external callers + tests; new code MUST NOT depend on them. `# TODO(2.0):` Phase 6 deletes the 3 wrappers once all external callers have migrated.
48. **[v2.0-beta] `_build_pr_body(state)` lives in `create_pr.py`** — Phase 3.3 moved `_build_pr_body` from `publish.py` to `create_pr.py` (signature unchanged). New code that needs the PR body builder MUST `from workflows.autocode_impl.nodes.create_pr import _build_pr_body` — NOT from `publish.py`. The `publish.py` module is now a backward-compat wrapper that doesn't define `_build_pr_body` directly (it just runs `node_push` + `node_create_pr` + `node_merge_pr` in sequence, and `node_create_pr` calls `_build_pr_body` internally).
49. **[v2.0-beta] `_is_path_safe(base_path, rel_path)` lives in `apply_patches.py`** — Phase 3.1 moved `_is_path_safe` from `write_files.py` to `apply_patches.py`. New code that needs the path-traversal guard MUST `from workflows.autocode_impl.nodes.apply_patches import _is_path_safe`. The `write_files.py` wrapper re-exports it for `import`-compatibility with existing callers, but new code SHOULD import from `apply_patches.py` directly. `write_new_files.py` already imports it this way.
50. **[v2.0-rc1] Use 4-phase debug structure (investigation → pattern → hypothesis → fix)** — `DEBUG_SYSTEM` in `constants.py` is now a 4-phase structured prompt inspired by obra/superpowers `systematic-debugging`. The LLM MUST declare its current phase in every JSON response via the `phase` field (enum: `"investigation"` / `"pattern"` / `"hypothesis"` / `"fix"` — enforced by `_DEBUG_JSON_SCHEMA`). When editing `DEBUG_SYSTEM`, preserve all 4 phases in order: (1) investigation — read error, reproduce mentally, check git diff, trace data flow; (2) pattern — find working example, compare line-by-line, identify the SINGLE difference; (3) hypothesis — form a SINGLE specific falsifiable hypothesis ("the bug is X because Y"); (4) fix — ONE targeted fix (no shotgun edits; if fix would touch >3 files, STOP — hypothesis is wrong). The `_DEBUG_JSON_SCHEMA` MUST keep `phase` as a required field with the enum constraint. The orchestrator uses the `phase` field to track iteration progression and detect stuck loops. See API.md § `node_systematic_debug`.
51. **[v2.0-rc1] Honor the architecture-question threshold (3+ consecutive failures)** — `nodes/debug.py` defines `_ARCHITECTURE_QUESTION_THRESHOLD = 3`. If `len(debug_history) >= 3` AND the last 3 entries ALL have `tests_passed=False`, `node_systematic_debug` bails with `tdd_status="max_retries_exceeded"` + procedural memory store (`memory.store(text=..., memory_type="procedural", importance=9, tags="tdd_failure,architecture_question,autocode,phase4", outcome="failed")`). This is DIFFERENT from #39 stuck detection (`route_after_run_tests` checks `last_test_error` signature equality — fires on the SAME error repeating). The architecture-question exit fires when DIFFERENT errors occur each iteration, suggesting the bug is architectural, not a fix-the-line bug. Inspired by superpowers Phase 4.5 pattern. When editing the architecture-question exit, preserve: (a) the threshold check `len(debug_history) >= _ARCHITECTURE_QUESTION_THRESHOLD`; (b) the `all(not entry.get("tests_passed", False) for entry in recent)` check; (c) the procedural memory store with the exact tag string; (d) the `tdd_status="max_retries_exceeded"` value; (e) the `"tdd": {"debug_history": debug_history}` in the return dict (preserves history on early exit). Both early-exit paths (architecture + max_retries) MUST preserve `debug_history` in the return dict.
52. **[v2.0-rc1] `node_summarize_context` writes `debug_summary` to `TDDState`** — Phase 4.3 added `node_summarize_context` (`nodes/summarize_context.py`) between `node_systematic_debug` and `node_apply_patches` in the debug loop. It reads `debug_history` via `_get_tdd`, reverses it (most recent first), renders each entry as a single sentence, tries `chonkie.SentenceChunker(chunk_size=512)` (lazy import, SOFT dependency) and returns the FIRST chunk; on ANY exception (including `ModuleNotFoundError`) falls back to `json.dumps(last_3_entries, ensure_ascii=False, default=str)`. The compressed string is written to the `debug_summary: str` field in `TDDState` (line 69 of `state.py`) — accessed via `_get_tdd(state, "debug_summary", "")`. The node does NOT mutate `debug_history` (the full history is preserved for the architecture-question exit check). chonkie is a SOFT dependency — do NOT make it a hard dep of `workflows.autocode_impl` (the workflow must remain importable in environments without chonkie installed, e.g., CI without optional deps). `# TODO(2.0-post):` No downstream node reads `debug_summary` yet — `node_systematic_debug` still uses the raw last-5-entries block. Migration to `debug_summary` is F3 in CHANGELOG.md § "Future Tracks (Post-2.0)".
53. **[v2.0-rc2] Import VCS functions from `vcs_ops.py`, not `git_ops` / `github_ops`** — Phase 5.1 consolidated the former `git_ops.py` (local operations: `_git_commit`, `_git_create_branch`) + `github_ops.py` (remote operations: `_github_pull`, `_github_push`, `_github_pr_create`, `_github_pr_comment`, `_github_pr_merge`, `_swarm_debug_consensus`) into a single unified `workflows/autocode_impl/vcs_ops.py` module, organized in 3 sections (Local operations / Remote operations / Swarm integration). `git_ops.py` + `github_ops.py` are KEPT as thin re-export wrappers for backward compat with external importers (so existing `from workflows.autocode_impl.git_ops import ...` and `from workflows.autocode_impl.github_ops import ...` calls still work), but new code MUST import from `vcs_ops.py` directly:
    - `from workflows.autocode_impl.vcs_ops import _git_commit, _git_create_branch` (Local operations section — was `git_ops.py`)
    - `from workflows.autocode_impl.vcs_ops import _github_pull, _github_push, _github_pr_create, _github_pr_comment, _github_pr_merge` (Remote operations section — was `github_ops.py`)
    - `from workflows.autocode_impl.vcs_ops import _swarm_debug_consensus` (Swarm integration section — was `github_ops.py`)

    The wrappers exist ONLY for backward compat with existing external callers; new code MUST NOT depend on them. `# TODO(2.0):` No phase is currently scheduled to delete the wrappers (they are cheap to maintain — pure re-exports). Resolves the v1.3 "2.0 Review Notes" item `git_ops.py + github_ops.py split → consider merging into unified vcs_ops.py`. See API.md § "`vcs_ops.py` — [v2.0-rc2] Unified VCS module (Phase 5.1)" and ARCHITECTURE.md § "[v1.3] Design Decision Notes" #2.

---

## 🚫 Anti-Patterns & Lessons Learned

> - **What happened:** `.bak` backup files were created on every file write (patch.py, write_files.py, helpers.py). This violated project rules and cluttered the repo with stale backups.
> - **Why it matters:** `.bak` files accumulate, confuse git status, and are unnecessary since git provides versioning. The project explicitly forbids `.bak` files.
> - **Fix:** Use atomic writes (tempfile.NamedTemporaryFile + os.replace) only. No `.bak` creation anywhere. Git is the backup.

> - **What happened:** `_git_snapshot()` called `git(action="snapshot")` which was removed from the git tool in the un-multiplex refactor. The call always failed silently.
> - **Why it matters:** The "pre-autocode snapshot" was supposed to be a safety net, but it never worked. Autocode ran without any snapshot protection.
> - **Fix:** Removed `_git_snapshot` entirely. The git branch itself is the safety net — if something goes wrong, `git checkout main` or `git revert` recovers. Future plan: add GitHub PR integration so autocode can create/fix PRs.

> - **What happened:** `files_map` was declared in `AutocodeState` and initialized to `{}`, but no node ever populated it. `node_analyze_impact` read it and found it empty, so impact analysis never ran.
> - **Why it matters:** Without impact analysis, autocode can't detect regressions or target specific tests. It always falls back to running the full test suite.
> - **Fix:** `node_write_files` now populates `files_map` with file snapshots (content_preview, md5, size) after writing files. `analyze_impact` will now actually run.

> - **What happened:** `node_analyze_impact` was declared `async def` but LangGraph `StateGraph.add_node` expects sync functions. The async node may fail silently or hang the graph.
> - **Why it matters:** Async-in-sync-graph is undefined behavior in LangGraph. The node may never execute, or may hang forever.
> - **Fix:** Converted to `def` (sync). Async calls (`parse_dependencies_from_string`, `get_targeted_tests`) are wrapped in `_run_async()` which creates a new event loop.

> - **What happened:** `node_brainstorm` merged `kg_files` into `files_update` but stored `state["files"]` (the original) instead of the merged result. Knowledge graph files were discarded.
> - **Why it matters:** KG context (relevant files from the dependency graph) was fetched but never made available to downstream nodes. The planner and executor never saw the KG files.
> - **Fix:** Store `files_update` (merged) instead of `state["files"]` (original).

> - **What happened:** `node_distill_memory` read `hypothesis` and `defense_note` (singular), but `node_systematic_debug` sets `root_cause` and `defense_notes` (plural). Both fields were always empty.
> - **Why it matters:** Procedural memory distillation never received root cause or defense notes — the most valuable debugging insights were discarded.
> - **Fix:** Changed to `root_cause` and `defense_notes` to match what debug.py actually sets. Same fix in `node_commit`.

> - **What happened:** The facade (`workflows/autocode.py`) was broken for 2 versions (v1.0.1 + v1.0.2). It imported 4 symbols (`AGENT_ROOT`, `route_after_brainstorm`, `route_after_debug`, `_git_snapshot`) that were removed from their source modules during the v1.0.1/v1.0.2 refactors. No test caught it because all 106 tests imported directly from `autocode_impl/`, never from the facade.
> - **Why it matters:** `import workflows.autocode` raised `ImportError`. `run_workflow(workflow_type="autocode")` was also broken (the dispatcher imports from the facade). The entire autocode workflow was unreachable through its documented public API for 2 versions.
> - **Fix:** [v1.1] Removed the 4 dead imports. Added facade contract tests (`test_facade.py`) that import the facade and exercise `run_workflow("autocode")`. This guards against silent facade breakage in future refactors.

> - **What happened:** `base.py`'s autocode branch did `graph = build_graph(); result = graph.invoke(state)`. But `build_graph()` returns an uncompiled `StateGraph`, which has no `.invoke()` method. This would crash with `AttributeError`.
> - **Why it matters:** Even if the facade imported correctly, `run_workflow("autocode")` would still crash at the invoke step.
> - **Fix:** [v1.1] `base.py` now uses `invoke_with_timeout(initial_state)` which calls `get_graph()` (returns compiled) internally. Also wires `cfg.autocode_graph_timeout`.

> - **What happened:** `route_after_write_files` only routed `fix`/`fix_error`/`refactor`/`improve`/`feature` to `node_analyze_impact`. `audit` and `edit` fell through to `node_verify`, skipping impact analysis entirely.
> - **Why it matters:** For `audit` especially, impact analysis IS the audit — skipping it made the audit task type misleading. For `edit`, the docs say it's "heavier than fix" but it skipped TDD, which was inconsistent.
> - **Fix:** [v1.1] Added `audit` and `edit` to the impact-analysis path. Found by cross-LLM review (DeepSeek, Mistral).

> - **What happened:** [v1.3] The autocode workflow had NO git push / PR / merge integration. Branch creation (`node_git_branch`) and commit (`node_commit`) were both local-only via `tools.git`. The A1/A2 research reports confirmed this — and flagged that `docs/tools/github/CHANGELOG.md` had a stale Phase 3 row incorrectly claiming "autocode's branch node uses raw `subprocess.run(['git', 'push', ...])`" (it didn't).
> - **Why it matters:** Operators running autocode in CI / on a remote server had to manually push branches and open PRs after every run. The github tool (with `push`, `pull`, `pr_create`, `pr_comment`, `pr_merge` actions) existed but was not wired in.
> - **Fix:** [v1.3] Added `node_publish` (between `node_commit` and `node_distill_memory`) and the `workflows/autocode_impl/github_ops.py` helper module. All operations are gated on config flags (`AUTOCODE_PUSH_ON_COMMIT`, `AUTOCODE_OPEN_PR`, `AUTOCODE_AUTO_MERGE`) and graceful-skip via `is_configured()`. With all flags OFF (the default), autocode behaves identically to v1.2.

> - **What happened:** [v1.3] The `branch` field was read by `nodes/branch.py` (line 55: `if state.get("branch"):`) and set by `nodes/plan.py` since v1.0, but was NOT declared in the `AutocodeState` TypedDict. This was a TypedDict drift — runtime worked (TypedDict with `total=False` doesn't enforce keys), but type-checkers and IDE autocomplete missed it.
> - **Why it matters:** TypedDict drift is a maintenance hazard — future editors reading `state.py` wouldn't know `branch` is a valid key, and might add a conflicting field with the same name.
> - **Fix:** [v1.3] Added `branch: str` declaration to `AutocodeState` (`state.py` line 94) and the default in `_default_state()` (`state.py` line 154). Pure type-safety fix — no runtime behavior change.

> - **What happened:** [v1.3] The debug node (`node_systematic_debug`) was single-LLM only — one provider proposed a `{root_cause, defense_notes, fix}` and the workflow applied it. There was no cross-model verification, so a single confused model could send the debug loop in circles.
> - **Why it matters:** Multi-model verification ("swarm") is the strongest signal we have for catching hallucinated root causes. Without it, autocode trusts whatever the executor LLM says.
> - **Fix:** [v1.3] Added optional swarm debug (`AUTOCODE_SWARM_DEBUG=1`) using a 2-run pattern: `swarm(consensus)` to propose, `swarm(vote)` to verify. Confidence is mapped from vote agreement: `unanimous → HIGH`, `majority → MEDIUM`, `split`/`disagreement` → `LOW`. The fix ALWAYS applies (non-blocking) — LOW confidence surfaces as an optional PR comment, not a workflow block. Rationale: blocking on a multi-LLM vote would add latency without improving correctness, given the debug loop's existing safety nets (`MAX_RETRIES`, stuck-detection, `node_verify` gate, git branch). Falls back to single-LLM debug if swarm is off / unavailable / fails.

---

*Last updated: 2026-07-11 (v2.0-rc2 — 2.0 refactor Phase 5 (VCS consolidation + cleanup): added NEVER DO #38 (never call `helpers._write_files()` — it is DEPRECATED as of Phase 5.3; use `node_apply_patches` + `node_write_new_files` instead); added ALWAYS DO #53 (import VCS functions from `vcs_ops.py`, not `git_ops` / `github_ops` — they are backward-compat thin re-export wrappers as of Phase 5.1). Prior v2.0-rc1 — Phase 4 (debug loop refactor): added NEVER DO #36-37 (never repeat a failed debug approach — check `debug_history` first; never wire `node_summarize_context` as a wrapper or skip it in the debug loop); added ALWAYS DO #50-52 (use 4-phase debug structure investigation → pattern → hypothesis → fix with required `phase` JSON field; honor the architecture-question threshold of 3+ consecutive `tests_passed=False` → `tdd_status="max_retries_exceeded"` + procedural memory store with `tags="tdd_failure,architecture_question,autocode,phase4"` — DIFFERENT from #39 stuck detection; `node_summarize_context` writes `debug_summary` to `TDDState` via chonkie `SentenceChunker` soft dep with JSON-of-last-3-entries fallback). See [ARCHITECTURE.md](ARCHITECTURE.md) for file maps, [API.md](API.md) for node details, [CHANGELOG.md](CHANGELOG.md) for version history + 7-phase refactor progress).*

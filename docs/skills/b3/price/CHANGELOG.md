<- Back to [PRICE Overview](../PRICE.md)

# 🗺️ Changelog — price skill

## ✅ Completed

### 📝 Version History

| Version | Date | Summary |
|---------|------|---------|
| **v1.7** | 2026-09-05 | **Opções tab (9th tab) — embeds Cadeia de Opções from b3/options skill.** Cross-skill integration: the price dashboard now calls `skills.b3.options.modes.dashboard.dashboard(underlying=ticker)` and embeds just the "Cadeia de Opções" tab sections (calls table + puts table + legend) as a new 9th "Opções" tab (group: Derivativos). Skips the P/C ratio, Volume por Strike, Exercicios, and IV tabs to keep the price dashboard compact. Wrapped in try/except — if the options skill import fails (circular dep / missing) or the underlying has no listed options, the tab shows a graceful "Sem opções disponíveis para este ticker" text section (the rest of the dashboard is unaffected). No new data source — the options skill already depends on `cotahist` + `sgs`, which are subsets of the price skill's `REQUIRED_SOURCES=["cotahist", "b3_dividends"]`. |
| **v1.6** | 2026-08-22 | **ADX/CCI/Williams %R indicators (3 new chart sections + signals table rows). Bid-Ask Spread tab (8th tab — spread absolute + spread % + bid/ask/close + liquidity KPIs). Table layout fix (width: auto).** Added 3 trend/cyclical indicators to `engines.py`: `compute_adx` (Wilder smoothing, 0-100 trend strength with +DM/-DM + TR), `compute_cci` (Commodity Channel Index, typical price = (H+L+C)/3, Lambert's 0.015 constant), `compute_williams_r` (momentum 0 to -100). `build_indicadores_sections` extended with `adx`/`cci`/`williams_r` params (default None for backward compat) + 3 collapsible chart sections (ADX with 25+75 reference lines, CCI with +/-100, Williams %R with -20/-80) + 3 rows in the signals table + 3 signal classifiers (`_adx_signal`, `_cci_signal`, `_wr_signal`). New `report/spread.py` builder with 4 sections (spread absoluto + spread percentual + bid/ask/close + liquidez KPI table). `ohlcv_series` SELECT extended to include `best_bid, best_ask`. New `compute_bid_ask_spread` + `compute_spread_pct` engine functions. Dashboard extended to 8 tabs (new 8th "Bid-Ask Spread" tab, group: Liquidez). `data_table` macro in `macros.html` changed from `width: 100%` to `width: auto; min-width: 100%` (tables shrink to content but never smaller than the container). New `tests/skills/b3/price/test_spread.py` with tests for `compute_bid_ask_spread` + `compute_spread_pct` correctness + `build_spread_sections` structure + graceful None handling. |
| **v1.5** | 2026-08-15 | **Retornos dividend return chart + Volume tab trade_count + contracts charts.** Retornos tab: added "Retorno de Dividendos" chart (green filled line = adjusted return − raw return = dividend contribution) + "Retorno de Dividendos" KPI row. Volume tab: added 2 new dual-axis charts — "Número de Negócios" (trade_count per day, shows market participation distinct from R$ volume) + "Quantidade de Ações" (contracts/shares traded per day, normalizes across price levels). Updated `ohlcv_series` to also fetch the `contracts` column from cotahist (was already in the DB but not queried). ROADMAP: added P8 (bid-ask spread analysis using best_bid/best_ask columns) + P9 (options skill using strike/maturity/strike_pts columns from COTAHIST). |
| **v1.4** | 2026-08-15 | **Cotação tab redesign: removed Volume Diário chart, added 5 new sections (price snapshot, period returns, annual returns, price histogram with heatmap, Início vs Fim chart).** The Volume Diário chart was removed from Cotação (the Volume tab already covers it). Added 4 pure-Python engine functions: `compute_price_snapshot`, `compute_period_returns`, `compute_annual_returns`, `compute_price_histogram` (30-bin distribution with heatmap colors blue→yellow→red + Point of Control + Value Area 70%). Added `negative_red` flag to the `data_table` macro (colors cells starting with "-" red). Added an inline `priceDatalabels` Chart.js plugin (no CDN) that renders data labels above each point for the Início vs Fim chart. All data computed from synced OHLCV (no hardcoded values). |
| **v1.3** | 2026-08-13 | **Fibonacci tab + dividend-adjusted returns (P2).** New 7th tab (group: Análise Técnica) with 10 sections organized BY SWING: for each swing (Swing_4 → Swing_12 → Swing_52), 3 collapsible tables — Níveis de Fibonacci (collapsed by default) + COMPRA trade setup (expanded by default) + VENDA trade setup (expanded by default) — then a single non-collapsible dividend-adjustments table. Added 4 pure-Python engine functions: `compute_adjusted_close` (backward dividend adjustment; v2: filters dividends by ISIN), `find_swing_extremes`, `compute_fibonacci_levels`, `compute_fibonacci_trade_setup`. Trade-setup formulas verified against the user's spreadsheet. 3 swing timeframes: Swing_4 (4 weeks / 30 days), Swing_12 (12 weeks / 90 days), Swing_52 (52 weeks / 365 days). v2: COMPRA + VENDA separate tables + Fibonacci chart removed. v3: extended `collapsible: True` to work with tables + added `collapsible_open` flag. v4: grouped by swing (not by category) — Níveis collapsed, COMPRA + VENDA expanded per swing; no repeated desc in title (only in description). Retornos tab now includes dividend-adjusted cumulative return chart + KPI row. `b3_dividends` added to `REQUIRED_SOURCES`. Dashboard is now 7 tabs. |
| **v1.2** | 2026-08-13 | **Indicadores tab — RSI + MACD + Stochastic + OBV + signals (v2: single-axis + price reference + collapsible).** New 6th tab (group: Preço) with a price reference chart at the top + 4 collapsible momentum oscillator charts + a signals summary table. Added 5 pure-Python engine functions to `engines.py`: `compute_ema` (Exponential Moving Average — MACD dependency), `compute_rsi` (Wilder's smoothing 14-day RSI, 0-100), `compute_macd` (12/26/9 — MACD line + signal line + histogram tuple), `compute_stochastic` (14/3/3 — %K + %D), `compute_obv` (On-Balance Volume cumulative signed volume). New `report/indicadores.py` builder emits 6 sections: (0) price reference chart (single-axis close-price line at the top so the user can see the price action once, then scroll through indicators), (1) RSI chart (0-100 with dashed 30/70 reference lines), (2) MACD chart (histogram bars colored green/red by sign + MACD/signal lines), (3) Stochastic chart (0-100 with dashed 20/80 reference lines), (4) OBV chart (filled purple line), (5) signals table classifying the latest reading of each indicator. **v2 fixes:** removed the dual-axis price overlay from the 4 indicator charts (was cluttering the read — same pattern as the Retornos v1.1 dual-axis removal); added a separate price reference chart at the top instead; made the 4 indicator charts collapsible (click the title to expand/collapse) via a new `collapsible: True` flag on chart sections + a `toggleChartCollapsible` JS function in dashboard.html that resizes charts on expand. Dashboard is now 6 tabs (was 5). No new data source — all indicators compute over the already-fetched OHLCV series. |
| **v1.1** | 2026-08-13 | **Retornos dual-axis removal + y-axis unit fix + dead-code cleanup.** (1) Removed the dual-axis price overlay (right axis) from the Retornos tab's cumulative-return + drawdown charts — they now show ONLY the return/drawdown series on a single left axis (cleaner reading; the Cotacao tab's Volume Diário chart keeps its dual-axis price overlay by design). (2) Fixed y-axis unit mismatch — chart data values were raw fractions (0.15) but the axis title said "(%)", so the displayed values didn't match the title. Now the chart data is multiplied by 100 (0.15 → 15.0) so the axis matches. The KPI table is unaffected (still uses `fmt_pct` on raw fractions). (3) Removed the dead `chartjs-chart-financial` CDN script tag from `dashboard.html` — the candlestick is rendered by the vanilla `_renderOHLCChart` helper (flagged via `chart_data._ohlc`), NOT the chartjs-chart-financial plugin (which was replaced because it forced a `time` x-scale that rendered blank without a date adapter). (4) Removed the dead `type: "candlestick"` macro branch from `macros.html` — no section emits this type; OHLC charts use `type: "chart"` with `_ohlc: True`. |
| v1.0 | 2026-08-06 | **Initial implementation.** 2 modes: dashboard (5-tab: Cotação / Médias Móveis / Volume / Retornos / Volatilidade) + quote (latest OHLCV + 52w range). Modular `_registry.py` + `modes/` + `report/` pattern (delegates to `skills/_base/`). Engines pre-built with 9 functions (ohlcv_series, latest_quote, SMA, returns, cumulative_returns, drawdowns, volatility, Bollinger Bands, MA crossovers, 52w range). Candlestick chart rendered via vanilla Chart.js `_renderOHLCChart` helper (flagged via `chart_data._ohlc`) — NOT the chartjs-chart-financial plugin (was removed during v1.0 dev because it forced a `time` x-scale that rendered blank). Range selector expanded to 7 buttons (Tudo/10A/5A/1A/6M/3M/1M). Sync guard wired via `required_sources=["cotahist"]` + `make_route()`. 4 tests. Read-only over `data_sources/b3/cotahist`. |

---

## 🔄 In Progress / Next Up

- **RSI + MACD** — momentum oscillators. RSI (14) overbought/oversold zones + MACD (12/26/9) crossovers. Would extend the Volatilidade tab with a "Momentum" subtab.
- **Intraday data** — currently only end-of-day OHLCV. Intraday would require a streaming/polling API (brapi or B3 market data feed).
- **Dividend-adjusted returns** — current returns are price-only. Adjusting for dividends + splits would give true total return (needs B3 dividends + corporate events data).
- **Options chain** — for tickers with listed options (PETR4, VALE3, etc.), surface the put/call ratio + implied volatility smile.

---

## 🚫 Deferred / Out of Scope

- **Multi-ticker comparison** — belongs in a future `compare` mode (mirror of `b3/index` compare). The candlestick chart is single-ticker by design.
- **Backtesting** — running a strategy on the historical price series belongs in `skills/cvm/backtest`, not here. The price skill only surfaces data; it doesn't simulate trades.
- **Technical pattern recognition** — automatic detection of head-and-shoulders, double tops, triangles, etc. would belong in a future `patterns` mode (or a separate `technical_analysis` skill).

---

*Last updated: 2026-09-05 (v1.7).*

"""skills — Domain expertise layer (FUTURE).

This package will contain procedural knowledge packages that combine
data sources (from data_sources/) with domain reasoning to produce
higher-level analysis (e.g., fundamental_analysis, portfolio_health).

Currently empty — the data source layer (data_sources/) is being built first.
Once data sources are stable, skills will be built on top.
"""

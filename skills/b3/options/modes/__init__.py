"""skills/b3/options/modes/__init__.py — Empty marker for the modes package.

Mode modules (dashboard.py) are auto-discovered by
skills._base.auto_discover_modes() via importlib. This file just marks the
directory as a regular package so `from skills.b3.options.modes.<mode>
import <fn>` works reliably across Python versions.
"""
